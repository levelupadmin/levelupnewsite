

## Plan: Replace Hero Background Image

Replace `src/assets/gvr-hero-bg.png` with the uploaded image (`venket-ram-desktop-p-1080.png`). This image is used in the footer CTA card's hero section.

### Change
- Copy `user-uploads://venket-ram-desktop-p-1080.png` → `src/assets/gvr-hero-bg.png` (overwrite)

No code changes needed — the existing import already references this file.

