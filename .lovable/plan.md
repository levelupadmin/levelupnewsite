

## Plan: Replace Pricing Card Image

Replace `src/assets/gvr-pricing.png` with the uploaded image (`image-123.png`). This is the instructor image shown in the pricing CTA card (section 10). No code changes needed — the existing import already references this file.

### Change
- Copy `user-uploads://image-123.png` → `src/assets/gvr-pricing.png` (overwrite)

No other images will be affected. The hero image remains unchanged.

